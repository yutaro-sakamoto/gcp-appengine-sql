runtime: python
env: flex

runtime_config:
  python_version: "3.11"

entrypoint: gunicorn -b :$PORT main:app

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 4

resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
